//
   Created by anton on 5/14/2015.

doctype
html
   head
      title Template
      link(rel="stylesheet",href="css/main.css")
   body
      section
         .controls
            .newGameCtrl
               input(type="checkbox" id="check")
               label.newMain(for="check")
                  .newMainText new
               .newContainer
                  .newPlate
                     .boardCellCircle(id="new-O")
                  .newPlate
                     .boardCellCross(id="new-X")
            .sizeCtrl
               .newMain
                  .newMainText(id="scale-Up") +
            .sizeCtrl
               .newMain
                  .newMainText(id="scale-Down") -
            .messages
               .messagesContainer
                  .newMainText(id="message") try to get 5 in line!
         .board
            - for (var x = 0; x < 15; x++)
               .boardRow
                  - for (var y = 0; y < 15; y++)
                     .boardCol
                        .boardCell(id=x + "-" + y)
      script(src="http://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js")
      script(src="js/bundle.js")